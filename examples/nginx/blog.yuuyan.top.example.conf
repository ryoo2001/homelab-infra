server {
    listen 80;
    server_name blog.yuuyan.top;

    # Public blog frontend. The Halo admin endpoint should be protected separately.
    location / {
        proxy_pass http://halo:8090;
        include /etc/nginx/conf.d/common-proxy-headers.conf;
    }
}
