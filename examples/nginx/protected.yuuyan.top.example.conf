server {
    listen 80;
    server_name protected.yuuyan.top;

    # This host should be protected by Cloudflare Access before it reaches the tunnel.
    location / {
        proxy_pass http://internal-service:8080;
        include /etc/nginx/conf.d/common-proxy-headers.conf;
    }
}
